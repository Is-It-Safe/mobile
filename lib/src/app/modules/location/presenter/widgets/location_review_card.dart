import 'package:flutter/material.dart';

class LocationReviewCard extends StatelessWidget {
  const LocationReviewCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
